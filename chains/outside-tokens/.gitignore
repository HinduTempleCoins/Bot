# Toolchain + build outputs — regenerated by `npm install` / `npx hardhat compile`. Never committed.
node_modules/
artifacts/
cache/
typechain-types/
coverage/
coverage.json

# Generated airdrop manifests (publish to IPFS/repo intentionally, don't auto-commit here).
claims.json
*.claims.json

# Never commit any key material or filled-in private env.
.env
.env.*
chains/outside-tokens/package-lock.json
